$('#comment-eraser').
  html('<%= j(render "comments/comment_card_btn_delete", comment: @comment, comments: @comments ) %>');
$('#comments-list-<%= @article.id %>').
  html('<%= j(render "comments/comments_list", comments: @comments, comment: @comment ) %>');
