<%= form_for [comment.article, comment], method: :delete, id: 'comment-delete', remote: true do |f| %>
  <%= f.submit "削除", class: 'p-0 px-1 mr-2 d-flex align-self-center btn-sm btn-info', data: { confirm: '本当にコメントを削除しますか？' } %>
<% end %>
