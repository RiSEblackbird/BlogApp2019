<%= form_for [current_user, current_user.following_relationships.find_by(following_id: @user.id)], html: { method: :delete } do |f| %>
  <%= f.submit "フォローを解除する", class: "btn btn-large btn-secondary follow-btn" %>
<% end %>
